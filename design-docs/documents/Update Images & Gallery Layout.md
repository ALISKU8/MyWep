I will place the images in their logical locations as requested:

1. **MEPCO Page (`mepco.html`):**

   * **Gallery:**

     * Update the first image to `شهادة مبكو.jpg` (already there, will confirm).

     * Update the second image to `صورة استلام شهادة مبكو.jpg` (replacing the placeholder).

2. **Jeddah Uni Page (`jeddah_uni.html`):**

   * **Gallery:** I will expand the gallery to include all 4 relevant images found in your folder:

     1. `شهادة-القياده-والاشراف.jpg` (Linked to "Volunteer Leader" achievement).
     2. `صورة الفريق.jpg` (Team Photo).
     3. `يوم-المهنه-شهادة-اشراف.jpg` (Supervision Certificate).
     4. `شهادة-التطوع.jpg` (Volunteering Certificate).

   * **Layout:** I will adjust the gallery layout in `style.css` to accommodate multiple images flexibly (removing the fixed height restriction) so they appear in a neat grid.

3. **Files to be modified:** `mepco.html`, `jeddah_uni.html`, `style.css`.

